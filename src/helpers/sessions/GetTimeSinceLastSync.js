import moment from "moment";

const GetDaysSinceLastSync = (recent_sync) => {
  // TODO: MAKE THIS ACTUALLY WORK - NOT READY YET THOUGH BECAUSE I HAVE NO SESSIONS
  return 10;
};

export default GetTimeSinceLastSync;
